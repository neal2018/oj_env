#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main() {
  cin.tie(nullptr)->sync_with_stdio(false);
  int n, k;
  string s;
  cin >> n >> k >> s;
  // ai = '0'
  // ndp[len][0] = dp[len-1][0] + dp[len-1][1] + dp[len][0]
  // ndp[len][1] = dp[len][1]
  //
}
