#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main() {
  cin.tie(nullptr)->sync_with_stdio(false);
  // auto func = [&](auto f, int a, int b){
  //   return f(a, b);
  // };
  // func(min, 1, 2);
}
