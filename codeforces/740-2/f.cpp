#include <bits/stdc++.h>
using namespace std;
#define ll long long

// hello
int main() {
  ll T, n, m;
  cin >> T;
  while (T--) {
    cin >> n >> m;
    vector<ll> x(m), y(m);
    for (ll i = 0; i < m; i++) {
      cin >> x[i] >> y[i];
    }
  }
}