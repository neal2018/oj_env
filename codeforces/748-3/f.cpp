#include <bits/stdc++.h>
using namespace std;
// https://space.bilibili.com/672328094 1:53
#define ll long long
int main() {
  cin.tie(nullptr)->sync_with_stdio(false);
  ll T, n, a, b;
  string x;
  cin >> T;
  while (T--) {
    cin >> n >> a >> b >> x;
    int dp[n][n][n][n];
    memset(dp, 0, sizeof dp);
  }
}